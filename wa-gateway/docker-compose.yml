services:
  kasrt-wa-gateway-lab:
    build:
      context: .
    container_name: kasrt-wa-gateway-lab
    env_file:
      - .env
    ports:
      - "127.0.0.1:${WA_LAB_HOST_PORT:-3010}:3010"
    volumes:
      - ./auth:/app/auth
      - ./data:/app/data
    restart: unless-stopped
